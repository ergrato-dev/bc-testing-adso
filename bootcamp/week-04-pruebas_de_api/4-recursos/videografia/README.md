# Videografía — Semana 4

- [Canal oficial SpringDeveloper en YouTube](https://www.youtube.com/@SpringSourceDev) (en inglés): busca "testing" o "MockMvc" para charlas sobre pruebas en Spring Boot.

> 💡 La mejor práctica de esta semana no está en un video: envía un JSON mal formado a **tu propio** API con `curl` y mira qué responde.
>
> ```bash
> curl -i -X POST http://localhost:8000/api/<recurso> -H 'Content-Type: application/json' -d '{"name":'
> ```
